export default 
   
    class User {
        constructor(userName,userIcon) {
            // invokes the setter
              this.userName = userName;
              this.userIcon = userIcon;
          }

    }

