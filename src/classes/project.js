export default    
    class Project {
        constructor(id,title,tasks,dueDateProj) {
          // invokes the setter
            this.id = id;
            this.title = title;
            this.tasks = tasks;
            this.dueDateProj = dueDateProj;
        }
    }

    